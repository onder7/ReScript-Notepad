import {
  some
} from "./chunk-H64I7E3G.js";
import "./chunk-BUSYA2B4.js";

// node_modules/rescript/lib/es6/js_dict.js
function get(dict, k) {
  if (k in dict) {
    return some(dict[k]);
  }
}
var unsafeDeleteKey = function(dict, key) {
  delete dict[key];
};
function entries(dict) {
  var keys = Object.keys(dict);
  var l = keys.length;
  var values2 = new Array(l);
  for (var i = 0; i < l; ++i) {
    var key = keys[i];
    values2[i] = [
      key,
      dict[key]
    ];
  }
  return values2;
}
function values(dict) {
  var keys = Object.keys(dict);
  var l = keys.length;
  var values$1 = new Array(l);
  for (var i = 0; i < l; ++i) {
    values$1[i] = dict[keys[i]];
  }
  return values$1;
}
function fromList(entries2) {
  var dict = {};
  var _x = entries2;
  while (true) {
    var x = _x;
    if (!x) {
      return dict;
    }
    var match = x.hd;
    dict[match[0]] = match[1];
    _x = x.tl;
    continue;
  }
  ;
}
function fromArray(entries2) {
  var dict = {};
  var l = entries2.length;
  for (var i = 0; i < l; ++i) {
    var match = entries2[i];
    dict[match[0]] = match[1];
  }
  return dict;
}
function map(f, source) {
  var target = {};
  var keys = Object.keys(source);
  var l = keys.length;
  for (var i = 0; i < l; ++i) {
    var key = keys[i];
    target[key] = f(source[key]);
  }
  return target;
}
export {
  entries,
  fromArray,
  fromList,
  get,
  map,
  unsafeDeleteKey,
  values
};
//# sourceMappingURL=rescript_lib_es6_js_dict__js.js.map
